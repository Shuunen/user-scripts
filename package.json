{
  "$schema": "https://json.schemastore.org/package",
  "author": "Romain Racamier-Lafon <romain.racamier@gmail.com>",
  "bugs": "https://github.com/Shuunen/user-scripts/issues",
  "description": "Personal heavily opinionated script I make to enhance my daily web experience",
  "devDependencies": {
    "@types/node": "^25.9.1",
    "@typescript/native-preview": "7.0.0-dev.20260627.2",
    "@vitest/coverage-v8": "^4.1.7",
    "@vitest/ui": "^4.1.7",
    "bun": "^1.3.14",
    "happy-dom": "^20.9.0",
    "oxfmt": "^0.56.0",
    "oxlint": "^1.66.0",
    "oxlint-tsgolint": "^0.23.0",
    "rough-notation": "latest",
    "tiny-glob": "^0.2.9",
    "turbo": "^2.9.14",
    "typescript": "~6.0.3",
    "vite": "^8.0.14",
    "vitest": "^4.1.7"
  },
  "homepage": "https://github.com/Shuunen/user-scripts",
  "keywords": [
    "user-script"
  ],
  "license": "GPL-3.0",
  "name": "@shuunen/user-scripts",
  "packageManager": "pnpm@10.34.4",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Shuunen/user-scripts.git"
  },
  "scripts": {
    "check": "turbo run lint:types lint:format lint:issues lint:scripts test:unit && echo check completed successfully",
    "lint": "turbo run lint:types lint:format lint:issues lint:scripts && echo lint completed successfully",
    "lint:format": "oxfmt --check && echo lint:format completed successfully || (echo Please run pnpm run lint:format:fix to automatically fix formatting issues && exit 1)",
    "lint:format:fix": "oxfmt --write && echo lint:format:fix completed successfully || (echo Please run pnpm run lint:format to check for remaining formatting issues && exit 1)",
    "lint:issues": "oxlint --type-aware --deny-warnings --report-unused-disable-directives && echo lint:issues completed successfully || (echo Please run pnpm run lint:issues:fix to automatically fix linting issues && exit 1)",
    "lint:issues:fix": "oxlint --fix --fix-suggestions --fix-dangerously --type-aware --deny-warnings --report-unused-disable-directives && echo lint:issues:fix completed successfully || (echo Please run pnpm run lint:issues to check for remaining linting issues && exit 1)",
    "lint:scripts": "bun src/bin/lint.cli.ts && echo lint:scripts completed successfully || (echo Please fix user-script issues && exit 1)",
    "lint:types": "tsgo -p tsconfig.app.json && tsgo -p tsconfig.node.json && echo lint:types completed successfully || (echo Please fix type errors and run pnpm run lint:types to check for remaining type issues && exit 1)",
    "test": "turbo run test:unit",
    "test:ui": "vitest --ui",
    "test:unit": "vitest run --coverage && echo test:unit completed successfully || (echo Please fix test errors and run pnpm run test:unit to check for remaining test issues && exit 1)",
    "test:watch": "vitest",
    "up:deps": "pnpm update --latest && echo Dependencies updated successfully"
  },
  "type": "module",
  "version": "2.6.6"
}
